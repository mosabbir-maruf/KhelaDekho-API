services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - XKEY=${XKEY}
      - V1_HOME_URL=${V1_HOME_URL}
      - V2_HOME_URL=${V2_HOME_URL}
      - V4_HOME_URL=${V4_HOME_URL}
      - KHELADEKHO_DEBUG=false
      - KHELADEKHO_LOG_LEVEL=INFO
    restart: unless-stopped
